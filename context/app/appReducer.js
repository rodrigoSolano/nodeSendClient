import {
  MOSTRATRAR_ALERTA,
  SUBIR_ARCHIVO_ERROR,
  SUBIR_ARCHIVO_EXITOSO,
  CREAR_ENLACE_EXITOSO,
  CREAR_ENLACE_ERROR,
} from "types"

const appReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
}